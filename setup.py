from setuptools import setup

setup(
    name = 'frontendstudies',
    version = '0.1.0',
    packages = ['frontendstudies'],
    install_requires = [
        'attrs',
        'scipy',
        'numpy',
        'root-numpy',
        'rootpy'
        ]
)
